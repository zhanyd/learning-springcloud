<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhanyd.springcloud.dao.PaymentDao">

    <resultMap type="com.zhanyd.springcloud.entities.Payment" id="BaseResultMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="serial" column="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <!--新增所有列-->
    <insert id="create" parameterType="Payment" keyProperty="id" useGeneratedKeys="true">
        insert into payment(serial)
        values (#{serial})
    </insert>

    <!--查询单个-->
    <select id="getPaymentById" resultMap="BaseResultMap" parameterType="Long">
        select
            id, serial
        from payment
        where id = #{id}
    </select>


    <!--通过主键修改数据-->
    <update id="update">
        update payment
        <set>
            <if test="serial != null and serial != ''">
                serial = #{serial},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from payment where id = #{id}
    </delete>

</mapper>